service:
  type: NodePort

persistence:
  enabled: true
  storageClass: "ebs-sc"
  accessMode: "ReadWriteOnce"
  size: "8Gi"

metrics:
  serviceMonitor:
    enabled: true
  prometheusRule:
    enabled: true
