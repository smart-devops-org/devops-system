auth:
  adminUser: admin
  adminPassword: "password"

replicaCount: 1

service:
  type: NodePort

  nodePorts:
    http: "30080"
    https: "30443"

tls:
  enabled: true

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
  prometheusRule:
    enabled: true

postgresql:
  enabled: false

externalDatabase:
  host: "postgresql.system.svc.cluster.local"
  port: 5432
  user: admin
  database: test
  password: "password"
