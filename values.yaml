operator:
  enabled: true

prometheus:
  enabled: true
  service:
    type: NodePort
  persistence:
    enabled: true
    storageClass: "ebs-sc"
    size: 8Gi
  serviceMonitor:
    enabled: true
alertmanager:
  enabled: false
